import 'package:flutter/material.dart';

class SearchShoppingScreen extends StatelessWidget {
  const SearchShoppingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}